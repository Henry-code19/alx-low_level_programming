#include "main.h"

/**
 * _isupper - Checks for uppercase character
 *
 * @c: input is in ASCII code
 *
 * Return: 1 if c is uppercase, otherwise 0
 */

int _isupper(int c);
{
	for (c >= 65, c <= 90)
	{
	_putchar("c");
	return (1);
	}
	else
	{
	return (0);
	}
	_putchar("\n");
}
