#ifndef _FUNCTION_LIKE_MACRO_H
#define _FUNCTION_LIKE_MACRO_H

/*
 * author: Henry Djan
 * Desc: header file that defines a function-lke-macro ABS(x)
 * that computes the absolutevalue of a number x
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
